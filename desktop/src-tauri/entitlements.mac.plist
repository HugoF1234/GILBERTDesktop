<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <!-- Allow audio input (microphone) -->
    <key>com.apple.security.device.audio-input</key>
    <true/>

    <!-- Allow audio recording from any source -->
    <key>com.apple.security.device.audio-video-bridging</key>
    <true/>

    <!-- Required for ScreenCaptureKit -->
    <key>com.apple.security.cs.allow-jit</key>
    <true/>

    <!-- Allow network access for API calls -->
    <key>com.apple.security.network.client</key>
    <true/>

    <!-- Allow reading user files -->
    <key>com.apple.security.files.user-selected.read-write</key>
    <true/>

    <!-- Allow writing to app's own documents directory -->
    <key>com.apple.security.files.downloads.read-write</key>
    <true/>

    <!-- Hardened runtime exceptions for Swift/ScreenCaptureKit -->
    <key>com.apple.security.cs.disable-library-validation</key>
    <true/>

    <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
    <true/>

    <!-- Automation for accessibility (optional, for detecting active windows) -->
    <key>com.apple.security.automation.apple-events</key>
    <true/>
</dict>
</plist>
